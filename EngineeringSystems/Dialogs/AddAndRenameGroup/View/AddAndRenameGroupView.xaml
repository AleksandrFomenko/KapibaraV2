<ui:FluentWindow x:Class="EngineeringSystems.Dialogs.AddAndRenameGroup.View.AddAndRenameGroupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://Kapibara/xaml"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        MinHeight="250"
        MinWidth="500"
        Height="250" 
        Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <ui:TitleBar 
            Title="{Binding Title}" 
            ShowMaximize="False" 
            ShowMinimize="False" />
        
        <ui:Card Grid.Row="1" Margin="15">
            <ui:TextBox
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                PlaceholderText="{Binding Description}"
                Text="{Binding NameGroup, UpdateSourceTrigger=PropertyChanged}" />
        </ui:Card>
        
        <ui:InfoBar Grid.Row="2"
                    Margin="5"
                    Title="{Binding InfoBarService.Title}"
                    IsOpen="{Binding InfoBarService.IsOpen}"
                    Message="{Binding InfoBarService.Message}"
                    Severity="{Binding InfoBarService.Severity, Mode=OneWay}" />

        
        <ui:Button 
            Grid.Row="3"
            Content="{Binding ButtonContent}"
            Appearance="Primary"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Icon="{ui:SymbolIcon ArrowReset20}"
            Command="{Binding ExecuteCommand}"/>
        
    </Grid>
</ui:FluentWindow>
