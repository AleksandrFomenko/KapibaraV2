<ui:FluentWindow
    x:Class="Settings.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://Kapibara/xaml"
    mc:Ignorable="d"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    Height="200"
    Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="Settings" />


        <ui:Card Grid.Row="1" Margin="10,0">
            <StackPanel>
                <TextBlock Text="Выбор темы"/>
                <Separator Height="10" Visibility="Hidden"/>
                <ComboBox
                    ItemsSource="{Binding Themes}"
                    SelectedItem="{Binding Theme, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </ui:Card>
        
        <ui:Card Grid.Row="2" Margin="10,0">
            <StackPanel>
                <TextBlock Text="Выбор background"/>
                <Separator Height="10" Visibility="Hidden"/>
                <ComboBox
                    ItemsSource="{Binding Backdrops}"
                    SelectedItem="{Binding Backdrop, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </ui:Card>
    </Grid>
</ui:FluentWindow>